import React from "react";
import styled from "styled-components";

const Card = (props) => <Container></Container>;

export default Card;

const Container = styled.View`
  background: pink;
  width: 100px;
  height: 100px;
  border-radius: 10px;
`;
